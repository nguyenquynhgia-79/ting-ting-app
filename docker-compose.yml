services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: tingting_user
      POSTGRES_PASSWORD: tingting_password
      POSTGRES_DB: tingting_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
